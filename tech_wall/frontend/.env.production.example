# ------------------------------------------------------------------------------
# AMBIENTE: PRODUÇÃO (.env.production.example)
# Execução: docker-compose.yaml (VPS via HTTPS)
# ------------------------------------------------------------------------------

# URL da API do Backend em Produção (DEVE SER HTTPS)
VITE_API_URL=https://api.seudominio.com.br
